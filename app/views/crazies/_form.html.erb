<%= form_with(model: crazy) do |form| %>
  <% if crazy.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(crazy.errors.count, "error") %> prohibited this crazy from being saved:</h2>

      <ul>
        <% crazy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :age, style: "display: block" %>
    <%= form.number_field :age %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
